bool hasAlternatingBits(int n) {
    int prev = n & 1;   // Last bit
    n = n >> 1;

    while (n > 0) {
        int curr = n & 1;   // Current last bit

        if (curr == prev)
            return false;

        prev = curr;
        n = n >> 1;
    }

    return true;
}
